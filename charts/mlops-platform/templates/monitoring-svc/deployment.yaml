apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-svc
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
    {{- include "mlops-platform.selectorLabels" (dict "component" "monitoring-svc") | nindent 4 }}
spec:
  replicas: {{ .Values.monitoringSvc.replicas }}
  selector:
    matchLabels:
      {{- include "mlops-platform.selectorLabels" (dict "component" "monitoring-svc") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mlops-platform.labels" . | nindent 8 }}
        {{- include "mlops-platform.selectorLabels" (dict "component" "monitoring-svc") | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: monitoring-svc
          image: {{ include "mlops-platform.image" (dict "svc" .Values.monitoringSvc "global" .Values.global "name" "monitoring-svc") }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.monitoringSvc.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: monitoring-svc-config
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.monitoringSvc.port }}
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.monitoringSvc.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.monitoringSvc.resources | nindent 12 }}
