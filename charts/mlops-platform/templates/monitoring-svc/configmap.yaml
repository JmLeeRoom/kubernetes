apiVersion: v1
kind: ConfigMap
metadata:
  name: monitoring-svc-config
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.monitoringSvc.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
