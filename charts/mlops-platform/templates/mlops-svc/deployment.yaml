apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-svc
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
    {{- include "mlops-platform.selectorLabels" (dict "component" "mlops-svc") | nindent 4 }}
spec:
  replicas: {{ .Values.mlopsSvc.replicas }}
  selector:
    matchLabels:
      {{- include "mlops-platform.selectorLabels" (dict "component" "mlops-svc") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mlops-platform.labels" . | nindent 8 }}
        {{- include "mlops-platform.selectorLabels" (dict "component" "mlops-svc") | nindent 8 }}
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: mlops-svc
          image: {{ include "mlops-platform.image" (dict "svc" .Values.mlopsSvc "global" .Values.global "name" "mlops-svc") }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.mlopsSvc.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: mlops-svc-config
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.mlopsSvc.port }}
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.mlopsSvc.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.mlopsSvc.resources | nindent 12 }}
