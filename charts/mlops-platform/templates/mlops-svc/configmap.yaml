apiVersion: v1
kind: ConfigMap
metadata:
  name: mlops-svc-config
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.mlopsSvc.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
