apiVersion: v1
kind: ConfigMap
metadata:
  name: serving-svc-config
  labels:
    {{- include "mlops-platform.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.servingSvc.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
